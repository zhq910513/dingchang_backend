# 整改清单（按文件归档，含优先级）
> 目标口径：**Models 约束 Schemas，Schemas 约束 API**；全链路字段/函数/返回结构闭环一致；不做旧字段兼容。  
> 本清单仅基于你本会话提供的源码静态审查结论（未使用线上样例/DDL 证据）。

## 优先级说明
- **P0（必改/必炸）**：启动即炸、接口必炸、核心链路无法完成、或会造成权限/数据口径严重错误。
- **P1（高风险/强烈建议）**：不改会导致明显功能缺失、数据不一致、前端口径混乱、或后续整改成本大。
- **P2（中低优先/体验与规范）**：风格一致性、可维护性、日志/命名/冗余约束等，不影响主链路但建议收口。

---

# 一、全局级承重墙问题（跨文件）

## 1) deps 返回结构在多处被错误解包（P0）
**事实源**：`app/api/deps.py`  
- `get_current_user_with_role()` 当前返回 **2 元组**：`(user, primary_role)`  
- `get_current_user_with_role_and_teams()` 当前返回 **4 元组**：`(user, primary_role, team_names, team_ids)`

**受影响文件（均存在错误解包/错误使用）**：
- `app/api/v1/users.py`：`user, main_role, _teams = me`（按 deps.py 会解包异常）
- `app/api/v1/field_config.py`：`_user, _role, _teams = me`（按 deps.py 会解包异常）
- `app/api/v1/customer_channel.py`：`user, _role, _teams = me`（按 deps.py 会解包异常；同时 list_* 里 me 未使用但依赖仍会注入）
- `app/api/v1/orders.py`：`list_orders()` 把 `get_current_user_with_role_and_teams` 的返回当“对象”取属性（role/team 取不到导致 ACL 错误/400）

**整改要求（必须统一一种约定）**：
- 统一 deps 的返回签名，并同步所有调用点；或
- 调用点统一改用 `get_current_user_with_role_and_teams` 并严格按 4 元组解包；或
- 彻底取消 3 元组用法，保证任何 API/Service 不再假设第三个值存在。

---

## 2) API 文件内自定义入参模型，违反“schemas 约束 API”（P1→若严格执行则 P0）
受影响文件：
- `app/api/v1/users.py`：`UserCreateIn` / `UserUpdateIn`
- `app/api/v1/field_config.py`：`FieldConfigUpsertIn`
- `app/api/v1/customer_channel.py`：`CustomerGroupCreateIn` / `ChannelGroupCreateIn`
- `app/api/v1/finance.py`：`FinanceOrderStatusUpdate`（虽然 finance.py 目前空壳）

**整改要求**：上述入参模型应迁移到对应 `app/schemas/*.py`，API 仅 import 使用。

---

# 二、订单模块（Orders）

## A) app/api/v1/orders.py
### P0
1. **GET /orders/{id} response_model 与真实返回结构不一致**  
   - 标注：`response_model=OrderOut`  
   - 实际返回：`load_order_detail_blocks()` 返回 blocks：`{"id","base","sections"}`  
   - 必须二选一：
     - 让该接口返回 `OrderOut`（走 read_model/to_order_out），或
     - 新增/调整 schema（如 `OrderDetailBlocksOut`）并修改 `response_model` 与前端契约。
2. **POST /orders 读取 schema 不存在字段**  
   - 代码读取：`payload.is_finished`、`payload.order_info`  
   - 但 `app/schemas/order.py::OrderCreate` 不含这些字段。
3. **_load_order_out 内 async 调用缺少 await**  
   - `db.execute(stmt)` 未 await，走到 create/update 返回时必炸。
4. **list_orders 中 current_user 使用方式错误**  
   - `get_current_user_with_role_and_teams` 返回 tuple，但 list_orders 当对象取 `.role_name/.team_names`，导致 ACL 误判/400。

### P1
5. **列表 items schema 与 read_model 输出不一致**（详见 `app/services/order_read_model.py`）  
6. `allow_finance` 参数未使用：`_ensure_orders_access(... allow_finance=False)`（逻辑残留）

---

## B) app/schemas/order.py
### P0
1. `OrderCreate` 缺少 orders.py 期望字段（若保留 orders.py 逻辑则必须补齐）：
   - 缺 `order_info`（以及是否允许 `is_finished`）
2. `OrderListResponse.items` 类型与 read_model 实现不一致：
   - `OrderListItemOut` 继承 `OrderOut`（必填字段多）
   - 但 read_model 列表项只填少数字段（见下）。

### P1
3. `OrderInfoIn/Out` 仅含 `remark`，但 orders.py 当前会维护大量 `OrderInfo` 计算字段（若确实不对外输出则 OK，但需明确：写入字段是否仍必要）。

---

## C) app/services/order_read_model.py
### P0
1. `orders_to_list_items()` 返回字段不足以构造 `OrderListItemOut(OrderOut)`  
   - 当前只填：id/created_at/status/audit_status/is_finished/customer_group_id/channel_group_id  
   - 但 `OrderOut` 必填：module/created_by/salesperson_id（至少）  
   - 必须二选一：
     - 列表项 schema 改为“精简专用 schema”（不继承 OrderOut），或
     - 列表项填充完整 OrderOut。

### P1
2. 详情与列表存在两套输出真源：orders.py 自己实现 `_load_order_out`；read_model 也实现 `to_order_out`。应收口“唯一真源”。

---

## D) app/services/order_detail_builder.py
### P0
1. 与 `orders.py` 的 `response_model=OrderOut` 冲突（同上）。

### P1
2. 时间格式输出到秒：`%Y-%m-%d %H:%M:%S`，而你全局要求前端展示/下载到 `YYYY-MM-DD`（需统一到同一口径）。
3. 输出结构中 sections/fields 是“自定义 blocks 契约”，需有对应 schema（否则无法做到 schemas 约束 API）。

---

## E) app/utils/order_image_urls.py + app/core/slot_field_config.py + app/services/storage.py
### P1
1. SlotImageItemOut 的 created_at/updated_at 当前用 `str(datetime)`，格式不稳定，且与 `YYYY-MM-DD` 口径不一致（建议统一）。
2. 未知 slot_key 的 title 规则：脏数据会以 slot_key 本身为 title（审计友好但前端需兜底展示）。

---

# 三、登录鉴权（Auth/Session）

## A) app/api/v1/auth.py
### P0
1. 返回 `LoginOut` 时传入 schema 不允许字段：`username`、`display_name`  
   - `app/schemas/auth.py::LoginOut` 仅允许：token/user_id/role_name/team_names/team_name  
   - 必须删字段或扩 schema（按“schemas 约束 API”规则二选一）。

### P1
2. `role_name` 取 `role_names[0]`，可能与 deps.py 的主角色优先级 `_ROLE_PRIORITY` 不一致（前后端显示与权限判断可能不一致）。
3. display_name 当前从 user.getattr('display_name')，但 User model 没这个字段（结果可能恒为空）。

---

## B) app/schemas/auth.py
### P1
1. 若前端确实需要 username/展示名，则应在 schema 中显式定义；否则前端不得依赖。
2. team_name/team_names 语义已写在 schema 注释里，需确保 service/API 均遵守（auth_service 已做）。

---

## C) app/services/auth_service.py
### P1
1. role_names 未排序；若希望主角色稳定，应在 service 端按优先级排序后返回（并让 API 直接取 primary）。

---

## D) app/models/session.py + app/api/deps.py
### P1
1. `UserSession.expired` 模型无默认值，但 auth_service 已写入 0；需确保任何创建 session 的路径都写 expired=0（否则插入失败风险）。

---

# 四、用户管理（Users）

## A) app/api/v1/users.py
### P0
1. 使用 `get_current_user_with_role` 解包 3 元组（按 deps.py 会炸）。
2. API 自定义入参 `UserCreateIn/UserUpdateIn`，违反“schemas 约束 API”。

### P1
3. `UserOut.from_orm(r)` 期望 `team_names: List[str]`，但 model `team_names` 是 CSV 字符串；需在输出前 split（或在 schema 层统一转换策略）。

---

## B) app/services/users_service.py
### P0（该文件整体旧口径，需系统性重写/对齐）
1. 使用 `User.display_name/phone/created_by` 等字段，但 `app/models/user.py` 不存在这些列。  
2. 使用 `Role.name`，但 `app/models/role.py` 字段为 `role_name`。  
3. 创建 `UserRole(... created_at, updated_at)`，但 `app/models/user_role.py` 无这些字段。  
4. 多处查询/筛选依赖不存在字段（如 `User.display_name.like`），必炸。

### P1
5. 团队字段写入必须符合 `TEAM_NAMES` 白名单，否则用户登录后访问模块会被 access_control 拒绝。

---

## C) app/schemas/user.py
### P1
1. `team_names` 为 List[str]，需要有统一的 CSV↔List 转换策略（在 service 或 schema 层实现）。

---

# 五、字段配置（Field Config）

## A) app/api/v1/field_config.py
### P0
1. `get_current_user_with_role` 解包 3 元组（按 deps.py 会炸）。
2. API 自定义 `FieldConfigUpsertIn`，违反“schemas 约束 API”。
3. upsert 入参缺少 `module`，但 model `FieldConfig.module` NOT NULL 且唯一键依赖 module。

---

## B) app/services/field_config_service.py
### P0
1. 使用 `FieldConfig.is_deleted` 过滤，但 `app/models/field_config.py::FieldConfig` 不存在 `is_deleted/deleted_at`。必炸。  
2. upsert 仅按 `field_name` 查找，但 model 唯一约束是 `(module, field_name)`，逻辑会错/覆盖错。

---

## C) app/models/field_config.py
### P1
1. 若确需软删口径，应补 `deleted_at/is_deleted`（并与 DB 一致），否则 service 必须去软删过滤。
2. FieldGroup/FieldGroupField 存在但当前 API/Service 未暴露/未使用（如需分区展示，后续应补全闭环）。

---

## D) app/schemas/field_config.py
### P1
1. 已与 FieldConfig 字段对齐（OK），但缺少 Upsert 入参 schema（若坚持 schemas 约束 API，应补齐）。

---

# 六、客户/渠道字典（Customer/Channel）

## A) app/api/v1/customer_channel.py
### P0
1. `get_current_user_with_role_and_teams` 解包 3 元组（按 deps.py 是 4 元组，会炸）。
2. API 自定义 `CustomerGroupCreateIn/ChannelGroupCreateIn`，违反 schemas 约束 API。
3. `/options` 返回结构与 `app/schemas/customer_channel.py` 完全不一致（必炸/必不对齐）。

### P1
4. create_customer 未传 contacts，但 CustomerGroup.contacts NOT NULL（需 service 兜底）。

---

## B) app/services/customer_channel_service.py
### P0
1. `create_customer_group` 未写 `contacts`，但 `CustomerGroup.contacts` 是 NOT NULL JSON（无默认则插入失败）。

### P1
2. list_* 使用 is_deleted computed（OK），但 schema/API 的 from_orm 能否工作取决于 schema 是否启用 ORM 模式（见下）。

---

## C) app/schemas/customer_channel.py
### P0
1. `OptionListOut/OptionItem` 与 API `/options` 输出结构不一致：  
   - schema：`items: [{id, group_name}]`  
   - API：`customers/channels: [{value,label}]`  
   必须统一（改 API 或改 schema）。

### P1
2. API 使用 `from_orm`，但 schema 未显式开启 ORM 模式（若运行环境是 Pydantic v1，默认会失败；若 v2 也需要 from_attributes 配置）。需统一策略：
   - 要么 schema 开启 ORM 输出；要么 API 不用 from_orm 手动构造。

---

# 七、财务（Finance）

## A) app/api/v1/finance.py
### P1
1. 当前文件为“空壳”，无任何 endpoint；仅定义 router 与 `FinanceOrderStatusUpdate`。  
   - 若要上线财务功能，必须补充实际路由与 service 调用。  
   - 若本轮不做财务模块，对外应明确“无财务 API”。

## B) app/schemas/finance.py
### P1
1. schema 仅包含 `is_rebate/is_paid`，与 `finance_record_new` 模型字段体系无对应。需要明确财务权威口径：
   - 财务是否仅操作 `order_new.is_rebate/is_paid`？
   - `finance_record_new` 是否对外/是否参与列表/详情？

## C) app/models/finance.py
### P1
1. 同一列 `order_id` 上出现两个 UniqueConstraint（名称不同，一个甚至叫 ix_*），逻辑上高度可疑；虽你口头保证 DDL 对齐，但建议核实与收口。

---

# 八、OCR 入库链路（启动期默认启用）

## A) app/main.py
### P1
1. 默认开启 `AUTO_SCHEMA_CHECK/AUTO_CREATE_TABLES/AUTO_ADD_COLUMNS`、`AUTO_SEED_*`、`OCR_POLL_ENABLED`：启动就会改库/跑 seed/跑 OCR poller。  
   - 若你希望“首次部署不误改 DB”，必须通过 env 管控或调整默认值（本清单只记录风险）。

## B) app/services/ocr_worker.py
### P1
1. 当前 OCR 写回位置仍是 `order_new.dynamic_data/ocr_raw_json`，未写入事实层 `order_slot_result_new/order_fact_new`（若你希望事实层成为真源，需接入）。

## C) app/services/ocr_cleaner.py
### ✅ OK（无整改）
- 明确删除 dl_*，日期/文本清洗口径清晰，符合新口径要求。

## D) app/services/baidu_ocr.py
### P2
1. access_token 进程内缓存（设计为性能优化，记录即可）。

## E) app/models/ocr_task.py / app/models/ocr_image_cache.py / app/models/image_ocr_result.py
### P1
- 字段与 worker 一致（OK）。注意 NOT NULL 字段需确保任何创建路径写全（目前未收齐“创建任务入口”，但 worker 假设任务存在且字段齐全）。

---

# 九、事实层（Order Fact Layer）

## A) app/models/order_slot_result.py
### P1
1. 事实表存在但当前生产链路未写入（OCR/订单 API 都未接入）。

## B) app/models/order_fact.py
### P1
1. 投影表存在但当前生产链路未维护（OCR/订单 API 都未接入）。

---

# 十、其它一致性与规范建议（P2）
- 多处重复实现 `_maybe_selectinload/_maybe_selectinload_nested`（orders.py/read_model/detail_builder），建议收口为单一工具函数。
- 多处时间格式不一致（YYYY-MM-DD vs 含时分秒 vs 直接 str(datetime)），建议统一输出规范并用同一工具函数。
- 多处 UniqueConstraint 命名可疑（name 与含义不一致），建议后续补 DDL 证据核对。

---

# 附：本会话已收齐文件清单（用于对照整改覆盖范围）
- app/main.py
- app/core/config.py
- app/core/constants.py
- app/core/db.py
- app/core/security.py
- app/core/slot_field_config.py
- app/core/access_control.py
- app/api/deps.py
- app/api/v1/__init__.py
- app/api/v1/auth.py
- app/api/v1/orders.py
- app/api/v1/users.py
- app/api/v1/customer_channel.py
- app/api/v1/field_config.py
- app/api/v1/finance.py
- app/schemas/auth.py
- app/schemas/order.py
- app/schemas/user.py
- app/schemas/customer_channel.py
- app/schemas/field_config.py
- app/schemas/finance.py
- app/services/auth_service.py
- app/services/order_read_model.py
- app/services/order_detail_builder.py
- app/services/storage.py
- app/services/users_service.py
- app/services/customer_channel_service.py
- app/services/field_config_service.py
- app/services/ocr_worker.py
- app/services/ocr_cleaner.py
- app/services/baidu_ocr.py
- app/utils/order_image_urls.py
- app/models/__init__.py
- app/models/session.py
- app/models/user.py
- app/models/role.py
- app/models/user_role.py
- app/models/order.py
- app/models/order_info.py
- app/models/order_slot_result.py
- app/models/order_fact.py
- app/models/image_file.py
- app/models/customer_group.py
- app/models/channel_group.py
- app/models/field_config.py
- app/models/finance.py
- app/models/ocr_task.py
- app/models/ocr_image_cache.py
- app/models/image_ocr_result.py

