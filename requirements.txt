fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0

SQLAlchemy[asyncio]==2.0.31
aiomysql==0.2.0
pymysql==1.1.1

# 使用 redis 官方库的 asyncio 支持
redis==5.0.7

pydantic==1.10.15
python-dotenv==1.0.1

# HTTP client（OCR 等会用到）
requests==2.32.3

# 上传文件（FastAPI form-data）
python-multipart==0.0.9

# 百度 BOS（可选，启用 BOS_ENABLED 时需要）
baidubce==0.8.3

anyio~=4.12.0
urllib3~=2.6.2
